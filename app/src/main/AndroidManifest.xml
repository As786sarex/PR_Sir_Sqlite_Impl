<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wildcardenter.myfab.pr_sir_front_end">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.BookByCsCourseActivity"
            android:label="Books Offered By CSE" />
        <activity android:name=".activities.DeptByPublisherActivity" />
        <activity android:name=".activities.EditAdaptActivity" />
        <activity
            android:name=".activities.ShowAdaptionActivity"
            android:label="All Book Adaption" />
        <activity android:name=".activities.EditTextActivity" />
        <activity
            android:name=".activities.ShowTextActivity"
            android:label="All Text Books" />
        <activity android:name=".activities.EditEnrollActivity" />
        <activity
            android:name=".activities.ShowEnrollActivity"
            android:label="All Enrollments" />
        <activity
            android:name=".activities.LandingActivity"
            android:label="Database Schema"
            android:theme="@style/MyAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CourseEditActivity" />
        <activity
            android:name=".activities.ShowCourseActivity"
            android:label="All Courses" />
        <activity android:name=".activities.StudentEditActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="All Students" />
    </application>

</manifest>